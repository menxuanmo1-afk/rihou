# 日后

记下每个小时实际做了什么，把「延迟满足」画出来。

手机用 **Safari** 打开：https://menxuanmo1-afk.github.io/rihou/ ，再点「分享 → 添加到主屏幕」。

改代码：在 Cursor 里改完 `git push`，一两分钟后网页更新。记录存在你自己的浏览器里，推送不会清空。

本地预览：

```bash
python3 serve.py
```
